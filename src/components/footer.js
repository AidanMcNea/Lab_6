import React, { Component } from "react"; //impoted react

export class Footer extends Component //auto imported export so as designed for export
{
    render(){

        //html code
        return(
            <div>
                <h1>This is my Footer component</h1>
            </div>
        );
    }
}